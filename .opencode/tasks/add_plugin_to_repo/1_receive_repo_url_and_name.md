# Step 1: Receive repo URL and name

The user provides a repository URL (e.g., `nvim-lualine/lualine.nvim`) and optionally a name for the plugin. If no name is given, proceed to derive it from the repo name.