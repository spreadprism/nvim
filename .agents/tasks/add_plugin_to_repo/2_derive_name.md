# Step 2: Derive name if needed

If the user didn't provide a name, derive it from the repository name. For example, `lualine.nvim` becomes `lualine`. Always remove prefixes/suffixes like `-nvim`, `.nvim`, or `nvim-`.